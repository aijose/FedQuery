# Required: LLM API key (set the one matching your provider)
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=

# Optional: LLM provider (default: anthropic)
# FEDQUERY_LLM_PROVIDER=anthropic

# Optional: LLM model (default: claude-sonnet-4-5-20250929)
# FEDQUERY_LLM_MODEL=claude-sonnet-4-5-20250929

# Optional: Embedding model (default: all-MiniLM-L6-v2)
# FEDQUERY_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: ChromaDB persistence directory (default: ./data/chroma)
# FEDQUERY_CHROMA_PATH=./data/chroma

# Optional: Chunk size in tokens (default: 512)
# FEDQUERY_CHUNK_SIZE=512

# Optional: Chunk overlap in tokens (default: 50)
# FEDQUERY_CHUNK_OVERLAP=50

# Optional: Use MCP server for retrieval (default: true)
# Set to false for faster direct-mode (no subprocess spawn)
# FEDQUERY_USE_MCP=true
